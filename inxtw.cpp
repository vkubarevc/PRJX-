#include<conio.h>
int main ()
{
  int a,b;
  printf("Enter two non-negative numbers\n";
         scanf(%d%d,&a,&b);
         if(a==b);
}
         printf("%d is equal to %d  \n",a,b);
         
}
else
 (
        printf("%d is not equal to %d",a,b);
}
   return 0;
   }
